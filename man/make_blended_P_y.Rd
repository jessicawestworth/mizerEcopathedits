% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/make_blended_sBH_FMort.R
\name{make_blended_P_y}
\alias{make_blended_P_y}
\title{Create a Blended Fishing Mortality Function for a Power-Law Harvest Rule}
\usage{
make_blended_P_y(
  params,
  size = NULL,
  length = NULL,
  t_max_blend,
  target_c,
  alpha_max = 1,
  t_steady,
  Type_Fmort = 1,
  gamma = 1.8,
  y = NULL
)
}
\arguments{
\item{params}{A \code{MizerParams} object.}

\item{size}{Optional size cut-offs passed to precompute_cutoff.}

\item{length}{Optional length cut-offs passed to precompute_cutoff.}

\item{t_max_blend}{Numeric. Time point at which maximum transition is reached.}

\item{target_c}{Numeric. Proportionality constant for Balanced Harvest (\eqn{F = c \cdot P}).}

\item{alpha_max}{Numeric (0 to 1). Maximum extent of transition (1.0 = total shift to sBH).}

\item{t_steady}{Numeric. Burn-in time before blending begins.}

\item{Type_Fmort}{Integer (1 or 2). Mode of calculating f_sBH:
\itemize{
\item 1: Keep the shape of the current gear-based F and rescale it so that
the biomass-weighted mean F above the cutoff equals the target.
\item 2: Rescale the peak-normalised gear selectivity x catchability
profile so that fully-selected individuals experience the target F.
}}

\item{gamma}{Numeric. Exponent on production in the harvest rule. \code{gamma = 1}
is the fixed exploitation ratio; \code{gamma = 1 + alpha} reproduces balanced
harvest at the reference state.}

\item{y}{Deprecated. Former name of \code{gamma}.}
}
\value{
A function compatible with \code{mizer}'s \code{FMort} rate slot.
}
\description{
As \code{\link[=make_blended_sBH_FMort]{make_blended_sBH_FMort()}}, but implements the more general harvest rule
\deqn{F_i = c\, P_i^{\gamma} / B_i,} equivalently \eqn{Y_i = c\,P_i^{\gamma}},
where \eqn{P_i} and \eqn{B_i} are the production and biomass of species
\eqn{i} above the size cutoff. \eqn{\gamma = 1} is the fixed exploitation
ratio \eqn{Y_i = cP_i}.
}
\section{Relation to balanced harvest}{

If the model satisfies \eqn{B_i = k P_i^{\alpha}} then setting
\eqn{\gamma = 1 + \alpha} gives \eqn{F_i = (c/k) P_i}, the balanced harvest
rule of \code{\link[=make_blended_sBH_FMort]{make_blended_sBH_FMort()}}. That equivalence holds only at the state
where the \eqn{B}-\eqn{P} fit was made: \eqn{B_i} in the denominator here is
the \emph{dynamic} biomass, so once the community responds to fishing the two
rules diverge. Note also that the fitted constant \eqn{k} is absorbed into
\eqn{c}, so \code{target_c} is not comparable across models or across values of
\code{gamma} unless it is recalibrated for each; see
\code{\link[=compute_LogP_LogB_slope]{compute_LogP_LogB_slope()}} and the "Species Level Balanced Harvest
Implementation" vignette.
}

\seealso{
\code{\link[=make_blended_sBH_FMort]{make_blended_sBH_FMort()}}
}
