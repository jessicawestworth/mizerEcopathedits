% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/Plot_Compute_LogP_LogB_LogY.R
\name{compute_LogP_LogB_slope}
\alias{compute_LogP_LogB_slope}
\title{Function that will compute the Biomass Production slope relationship
may be used in future for detecting changes in this relationship as the
fishing mortality is adjusted}
\usage{
compute_LogP_LogB_slope(
  params,
  size = NULL,
  length = NULL,
  n = initialN(params),
  n_pp = initialNResource(params),
  n_other = initialNOther(params)
)
}
\value{
A list with the fitted \code{model}, its \code{intercept} and \code{slope}, and the
per-species \code{data} the fit was made from.
}
\description{
The fit is \eqn{\log B = \log k + \alpha \log P} on natural logs, so the
returned \code{slope} is \eqn{\alpha} and the fitted proportionality constant is
\eqn{k = \exp(\mathrm{intercept})}. Note that \eqn{k} is \emph{not} the harvest
rule constant \eqn{c}: see the "Species Level Balanced Harvest
Implementation" vignette.
}
