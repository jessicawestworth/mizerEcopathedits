% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/make_blended_sBH_FMort.R
\name{make_blended_sBH_FMort}
\alias{make_blended_sBH_FMort}
\title{Create a Blended Fishing Mortality Function for Balanced Harvesting Transitions}
\usage{
make_blended_sBH_FMort(
  params,
  size = NULL,
  length = NULL,
  t_max_blend,
  target_c,
  alpha_max = 1,
  t_steady,
  Type_Fmort = 1
)
}
\arguments{
\item{params}{A \code{MizerParams} object.}

\item{size}{Optional size cut-offs passed to precompute_cutoff.}

\item{length}{Optional length cut-offs passed to precompute_cutoff.}

\item{t_max_blend}{Numeric. Time point at which maximum transition is reached.}

\item{target_c}{Numeric. Proportionality constant for Balanced Harvest (\eqn{F = c \cdot P}).}

\item{alpha_max}{Numeric (0 to 1). Maximum extent of transition (1.0 = total shift to sBH).}

\item{t_steady}{Numeric. Burn-in time before blending begins.}

\item{Type_Fmort}{Integer (1 or 2). Mode of calculating f_sBH:
\itemize{
\item 1: Keep the shape of the current gear-based F and rescale it so that
the biomass-weighted mean F above the cutoff equals the target.
\item 2: Rescale the peak-normalised gear selectivity x catchability
profile so that fully-selected individuals experience the target F.
}}
}
\value{
A function compatible with \code{mizer}'s \code{FMort} rate slot.
}
\description{
This function returns a customized mortality function for use within
a \code{mizer} simulation. It creates a linear transition from
traditional gear-based fishing mortality to species-level Balanced
Harvesting (sBH) based on species production.
}
\section{Limitation of \code{Type_Fmort = 1}}{

Because Type 1 rescales the existing fishing mortality, a species with zero
status-quo F stays unfished for the whole projection, and a species with a
very small status-quo F receives a very large multiplier. Balanced harvest as
a policy asks that every species be harvested in proportion to its
productivity regardless of what the current fleet does, so Type 1 cannot
express the rule for species the fleet does not currently target. Use
\code{Type_Fmort = 2} if that matters for the question being asked.
}

\section{Definition of production}{

Production is the somatic production \eqn{\int N_i(w) g_i(w) dw} above the
size cutoff. This matches the Ecopath production of \code{\link[=getProduction]{getProduction()}}
restricted to that size range, because the other component of the Ecopath
production — offspring biomass, see \code{\link[=getOffspringProduction]{getOffspringProduction()}} — enters the
spectrum at \code{w_min}, far below any cutoff of interest.
}

