% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/ageDensity.R
\name{preprocess_length_at_age}
\alias{preprocess_length_at_age}
\title{Pre-process length-at-age data frame}
\usage{
preprocess_length_at_age(params, species, age_at_length)
}
\arguments{
\item{params}{A \code{mizer::MizerParams} object, with scientific names contained
in the species_paramsncolumn "Scientific_name"}

\item{species}{Species name as in \code{species_params(params)$species}.}

\item{age_at_length}{Data frame with at least the columns \code{Scientific_name},
\code{Quarter}, \code{LngtClass}, \code{Age}, and \code{CANoAtLngt}.}
}
\value{
A data frame with the columns described above, one row per
quarter-length-age combination.
}
\description{
Filters and aggregates age-at-length data for a species, rounding lengths down
to the nearest cm and aggregating counts by quarter. Returns a tidy data frame
with columns \code{survey_date}, \code{Length}, \code{K}, and \code{count}.
}
\examples{
# Using package data would typically look like:
# df <- preprocess_length_at_age(params, species = "Cod", age_at_length = your_df)
}
